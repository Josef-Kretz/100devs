// Create a function that takes in 3 numbers. The function
// should return the first number multiplied by the second and
// then subtract the third. Console.log() the resulting value

function math_hw(num1, num2, num3)
{
  let result = (num1 * num2) - num3
  console.log(result)
}

console.log("Use math_hw(n1,n2,n3) to do some math!")
